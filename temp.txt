with fileinput.FileInput(conf_file, inplace=True, backup='.bak', mode='r') as file:
        for line in file:
            for attr in attributes:
                print(line.replace(text_to_search, replacement_text), end='')